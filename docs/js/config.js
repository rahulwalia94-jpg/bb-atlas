// BB Atlas configuration.
// After deploying the tutor backend on Render, paste its URL here, e.g.
//   API_URL: "https://bb-atlas-tutor.onrender.com"
// Leave empty to run fully offline (content, quizzes, dominoes all work;
// the tutor panel will explain it isn't connected yet).
window.BB_CONFIG = {
  API_URL: "",
};
